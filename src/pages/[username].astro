---
import Layout from '../layouts/Layout.astro';
import LinkCard from '../components/LinkCard.astro';
import AchievementCard from '../components/AchievementCard.astro';
import { getProfileByUsername, getPublicProfiles } from '../data';

export async function getStaticPaths() {
  const profiles = getPublicProfiles();
  
  return profiles.map(profile => ({
    params: { username: profile.username },
    props: { profile }
  }));
}

const { profile } = Astro.props;

// Prepare Open Graph data
const ogTitle = profile.ogTitle || `${profile.name} - Linkarooie`;
const ogDescription = profile.ogDescription || profile.description;
const ogImage = profile.ogImageUrl;
---

<Layout 
  title={`${profile.name} - Linkarooie`}
  description={profile.description}
  ogImage={ogImage}
  username={profile.username}
>
  <div class="max-w-4xl mx-auto pb-12">
    <!-- Banner & Avatar -->
    <div class="relative">
      <div class="w-full h-48 sm:h-64 md:h-80 overflow-hidden">
        <img 
          src={profile.bannerUrl} 
          alt={`${profile.name}'s banner`}
          class="w-full h-full object-cover"
        />
      </div>
      
      <div class="absolute bottom-0 left-1/2 transform -translate-x-1/2 translate-y-1/2">
        <div class="w-24 h-24 md:w-32 md:h-32 rounded-full overflow-hidden border-4 border-white bg-white">
          <img 
            src={profile.avatarUrl} 
            alt={`${profile.name}'s avatar`}
            class="w-full h-full object-cover"
          />
        </div>
      </div>
    </div>
    
    <!-- Profile Info -->
    <div class="mt-16 md:mt-20 text-center px-4">
      <h1 class="text-3xl font-bold text-gray-900">{profile.name}</h1>
      <p class="text-gray-600">@{profile.username}</p>
      
      <p class="mt-4 text-xl text-gray-700 max-w-2xl mx-auto">{profile.description}</p>
      
      <!-- Social Links -->
      {profile.socialLinks.length > 0 && (
        <div class="flex justify-center space-x-4 mt-4">
          {profile.socialLinks.map(social => (
            <a 
              href={social.url} 
              target="_blank" 
              rel="noopener noreferrer"
              class="text-gray-600 hover:text-gray-900"
              data-track="true"
              data-id={`social-${social.platform}`}
              data-type="social"
            >
              <i class={`fab fa-${social.platform} text-2xl`}></i>
              <span class="sr-only">{social.platform}</span>
            </a>
          ))}
        </div>
      )}
      
      <!-- Tags -->
      {profile.tags.length > 0 && (
        <div class="flex flex-wrap justify-center gap-2 mt-6">
          {profile.tags.map(tag => (
            <span class="px-3 py-1 bg-blue-100 text-blue-800 rounded-full text-sm">
              {tag}
            </span>
          ))}
        </div>
      )}
      
      <!-- Analytics Link -->
      <div class="mt-6">
        <a href={`/${profile.username}/analytics`} class="text-blue-600 hover:underline inline-flex items-center">
          <i class="fas fa-chart-line mr-2"></i>
          View Analytics
        </a>
      </div>
    </div>
    
    <!-- Links Section -->
    <section class="mt-12 px-4">
      <h2 class="text-2xl font-bold text-gray-900 mb-6">Links</h2>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        {profile.links.filter(link => !link.hidden).map(link => (
          <LinkCard link={link} />
        ))}
      </div>
      
      {profile.links.filter(link => link.hidden).map(link => (
        <LinkCard link={link} />
      ))}
    </section>
    
    <!-- Achievements Section -->
    <section class="mt-12 px-4">
      <h2 class="text-2xl font-bold text-gray-900 mb-6">Achievements</h2>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        {profile.achievements.filter(achievement => !achievement.hidden).map(achievement => (
          <AchievementCard achievement={achievement} />
        ))}
      </div>
      
      {profile.achievements.filter(achievement => achievement.hidden).map(achievement => (
        <AchievementCard achievement={achievement} />
      ))}
    </section>
  </div>
</Layout>